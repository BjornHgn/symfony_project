{% extends 'base.html.twig' %}

{% block title %}{{ article.nom }} - Détails{% endblock %}

{% block body %}
    <div style="display: flex; max-width: 1200px; margin: auto; padding: 40px; box-sizing: border-box; gap: 40px;">
        <!-- Partie gauche : Image du vêtement -->
        <div style="flex: 1; display: flex; justify-content: center; align-items: center;">
            <img src="{{ article.image }}" alt="Image de {{ article.nom }}" 
                 style="width: 400px; height: 400px; object-fit: contain; border-radius: 10px;">
        </div>

        <!-- Partie droite : Détails du vêtement -->
                <div style="flex: 1; display: flex; flex-direction: column; justify-content: center;">
            <h1 style="margin-bottom: 15px;">{{ article.nom }}</h1>

            <p style="margin: 10px 0; font-size: 18px; color: #333;">
                <strong>Prix :</strong> 
                <span style="font-size: 22px; font-weight: bold; color:#D8C3A5;">{{ article.prix }} €</span>
            </p>

            <p style="color: #555;">{{ article.description }}</p>

            <!-- Sélection de la taille -->
            <label for="size" style="margin-top: 10px; font-weight: bold;">Taille :</label>
            <select id="size" name="size" style="padding: 8px; margin-top: 5px; width: 200px; border-radius: 5px;">
                <option value="S">S</option>
                <option value="M">M</option>
                <option value="L">L</option>
                <option value="XL">XL</option>
            </select>

            <!-- Sélection de la couleur -->
            <label for="color" style="margin-top: 10px; font-weight: bold;">Couleur :</label>

            <!-- Sélection de la quantité -->
            <label for="quantity" style="margin-top: 10px; font-weight: bold;">Quantité :</label>
            <input type="number" id="quantity" name="quantity" value="1" min="1" 
                   style="padding: 8px; margin-top: 5px; width: 70px; text-align: center; border-radius: 5px;">

            <!-- Bouton Ajouter au panier -->
            <button type="submit" 
                    style="display: block; margin-top: 20px; padding: 12px; background: #D8C3A5; color: white; 
                           border: none; border-radius: 5px; font-size: 16px; font-weight: bold; cursor: pointer; 
                           width: 200px; transition: 0.3s;">
                🛒 Ajouter au panier
            </button>
        </div>
    </div>
{% endblock %}
